from django.apps import AppConfig
""" This app outlines the midleware function in this service based web application
it contains all of the API functions as well as the admin configs
"""

class BaseConfig(AppConfig):
    name = 'base'
